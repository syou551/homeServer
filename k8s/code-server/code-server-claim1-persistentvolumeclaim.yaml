apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: code-server-claim1
  name: code-server-claim1
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
