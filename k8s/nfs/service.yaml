apiVersion: v1
kind: Service
metadata:
  labels:
    app: nfs-server
  name: nfs-server-svc
spec:
  type: ClusterIP
  ports:
  - name: nfs-2049
    port: 2049
    protocol: TCP
    targetPort: 2049
  selector:
    app: nfs-server
