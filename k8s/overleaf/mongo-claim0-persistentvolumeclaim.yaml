apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-claim0
  name: mongo-claim0
spec:
  storageClassname: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
