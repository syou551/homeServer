apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: back-claim0
  name: back-claim0
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
